/**
 * @fileoverview RingoJS provides native JSON support, so applications don't
 * need to require this package in order to make use of JSON. It is here for
 * backwards compatibility only.
 */

/**
 * Encodes a JavaScript object to a JSON encoded String.
 * @param {Object} obj convert an object to a JSON string
 * @returns {String} a JSON string representing the object
 * @name JSON.stringify
 */

/**
 * Parses a JSON string to a JavaScript object.
 * @param {String} str a JSON string
 * @returns {Object} the Object decoded from str
 * @name JSON.parse
 */
